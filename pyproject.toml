[project]
name = "pickone-medical"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.15"
dependencies = [
    "flask>=3.1.2",
    "pwdlib[argon2]>=0.3.0",
]
